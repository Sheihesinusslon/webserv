# host forms RFC 3986 / RFC 6874 allow: reg-name marks and bracketed IP-literals
server {
	listen my_host.local:8500;
	listen [::ffff:192.0.2.1]:8501;
	listen [fe80::1%eth0]:8502;
	listen [::1]:8503;
	location / {
		allow_methods GET;
	}
}
