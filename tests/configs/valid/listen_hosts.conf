# host forms nginx documents and RFC 3986 / RFC 6874 allow
server {
	listen my_host.local:8500;
	listen [::ffff:192.0.2.1]:8501;
	listen [fe80::1%eth0]:8502;
	listen [::1]:8503;
	listen [::1];
	listen [::]:8504;
	location / {
		allow_methods GET;
	}
}
