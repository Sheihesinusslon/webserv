# leading comment
server {          # after brace
	listen 8105;  # after directive
	# whole line
	root www/site;
	location / {  # in a block
		allow_methods GET;
	}
}
# trailing comment with no newline
