# a named block and a nameless default may share a listener: the Host header
# still tells them apart, and the nameless one catches everything else
server {
	listen 8302;
	server_name named.test;
	location / {
		allow_methods GET;
	}
}

server {
	listen 8302;
	location / {
		allow_methods GET;
	}
}
